# Manual Steps

* `maxmind_apikey` and `maxmind_userid` secrets need to be added to the Key Vault
* `ProxyCheck:ApiKey` secret needs to be added to the Key Vault (ProxyCheck.io API key for IP risk assessment)
