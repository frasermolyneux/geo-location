name: Build and Test

on:
  push:
    branches:
      - "feature/**"
      - "bugfix/**"
      - "hotfix/**"

permissions: {}

jobs:
  build-and-test:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: frasermolyneux/actions/dotnet-ci@dotnet-ci/v1.3
        with:
          dotnet-version: |
            9.0.x
            10.0.x
          src-folder: "src"

      - name: Install Playwright browsers
        run: pwsh src/MX.GeoLocation.Web.IntegrationTests/bin/Release/net9.0/playwright.ps1 install --with-deps chromium

      - name: Run integration tests
        run: dotnet test src/MX.GeoLocation.sln --configuration Release --no-build --filter "FullyQualifiedName~IntegrationTests"

  terraform-plan-dev:
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    environment: Development
    concurrency:
      group: ${{ github.repository }}-dev
    steps:
      - uses: frasermolyneux/actions/terraform-plan@main
        with:
          terraform-folder: "terraform"
          terraform-var-file: "tfvars/dev.tfvars"
          terraform-backend-file: "backends/dev.backend.hcl"
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
